# AGENTS.md

## Project / 專案名稱
AI Pre-Shipment Risk Decision Agent

## Project Goal / 專案目標
這個專案是公司內部 AI 競賽用的 Demo。

目標是先做出一個 **可執行、可展示** 的 prototype，展示 AI agent 如何在 ES / sample 出貨前，協助做 shipment risk 判斷。

這個 Demo 希望模擬 PM / AE / DQA 的工作流程，讓 agent 可以：
- 盤點 actual system / firmware information
- 與 tracking sheet 做比對
- 檢查 known issues
- 產出 shipment recommendation

## Current Stage / 目前階段
目前是早期 Demo 雛型，不是 production system。

現階段優先順序：
1. 先做得出來（runnable）
2. 先有一個 end-to-end flow
3. 保持簡單
4. 後續容易修改與擴充

請不要 over-engineer。

---

## Working Mode / 工作模式
Codex 在這個專案中工作時，請遵守以下順序：

1. **先查看 `input_data/` 內有哪些檔案**
2. **先列出每個 Excel workbook 的 sheet 名稱**
3. **先判斷哪些 sheets 可能屬於：**
   - expected configuration
   - actual system / firmware information
   - known issues
   - release note related content
4. **先提出欄位 mapping 建議**
5. **再開始寫 parser / compare / decision code**
6. **Version 1 先做最小 runnable flow，不要一次做太大**

如果資料格式不一致，第一版可接受：
- partial parsing
- simplified assumptions
- fake data 補齊缺少欄位
- manual mapping

---

## Input Data / 輸入資料
本專案資料夾內可能會放 PM / AE 提供的真實 Excel 檔案，作為 early reference data。

常見輸入可能包括：
- tracking sheet
- system information
- known issues
- release note summary

請先以 `input_data/` 內現有檔案為主。

如果真實 Excel 內容格式不一致，第一版可先用「可跑通 Demo」為優先，不需要一次處理所有情況。

---

## Expected Demo Flow / 預期 Demo 流程
Version 1 預期先完成以下流程：

1. 讀取 available input files
2. 抽取 expected configuration
3. 抽取 actual system / firmware information
4. 找出 mismatches
5. 找出 matched known issues
6. 評估 risk level
7. 輸出 shipment recommendation

---

## Recommendation Values
- Go
- Conditional Go
- No-Go

## Risk Level Values
- Low
- Medium
- High

---

## Practical Development Rules / 實作原則
- 使用 Python
- 模組保持小而實用
- 優先 readable code，不要過度抽象
- 註解清楚即可
- 邏輯要容易調整
- Version 1 避免不必要 framework
- 先求 end-to-end flow 可執行
- 若需要 UI，優先考慮簡單的 Streamlit
- 若 SSH / live command collection 太花時間，Version 1 可先以 local text / parsed Excel data 取代

---

## Suggested Files / 建議檔案結構
- `input_data/`
- `output/`
- `parser.py`
- `comparator.py`
- `issue_matcher.py`
- `decision_engine.py`
- `models.py`
- `demo.py` or `app.py`
- `sample_data/`

如有需要，可增加：
- `excel_inspector.py`
- `field_mapper.py`

---

## First Implementation Target / 第一版實作目標
第一版希望先做到：

- 能讀取現有 Excel / text inputs
- 能列出 workbook / sheet 結構
- 能抽出部分 useful fields
- 能做簡單 compare
- 能做簡單 known issue matching
- 能輸出 JSON 或畫面結果

---

## Expected Output Structure / 預期輸出欄位
輸出至少應包含：

- `project_name`
- `actual_config`
- `expected_config`
- `mismatch_items`
- `matched_known_issues`
- `risk_level`
- `recommendation`
- `action_items`

---

## Decision Logic Direction / 判斷邏輯方向
先用簡單明確規則：
- 關鍵 config mismatch -> 至少 Medium risk
- 命中 blocking known issue -> High risk
- BIOS / BMC mismatch 且 issue 相關 -> No-Go
- 沒 mismatch 且沒 issue -> Go

第一版可先用 rule-based logic。
後續若需要，再加入更進一步的 LLM reasoning。

---

## Important Notes / 重要提醒
- 第一版先專注一個小型 runnable end-to-end flow
- 必要時可使用 fake data 或 simplified assumptions
- 真實檔案先視為 reference inputs
- 不需要先做 deployment / authentication / production-grade permission control
- 結果要像 shipment decision assistant，不要只像 file parser
- 請優先幫助使用者快速做出可 demo 的版本

---

## When Unsure / 不確定時
如果遇到資料格式不明、欄位名稱不一致、資訊不足的情況：

1. 先列出觀察到的結構
2. 提出合理 mapping 建議
3. 用最小假設先讓 Demo 可跑
4. 保留程式結構讓後續容易修正